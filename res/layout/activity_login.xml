<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LoginActivity" >

    <EditText
        android:id="@+id/text_login_user"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerInParent="false"
        android:layout_marginTop="28dp"
        android:ems="10"
        android:hint="@string/nickname"
        android:inputType="textNoSuggestions">

        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/text_login_pass"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/text_login_user"
        android:layout_alignRight="@id/text_login_user"
        android:layout_below="@id/text_login_user"
        android:layout_marginTop="14dp"
        android:ems="10"
        android:hint="@string/password"
        android:inputType="textPassword"></EditText>

    <Button
        android:id="@+id/button_login"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/text_login_pass"
        android:layout_alignRight="@+id/text_login_pass"
        android:layout_below="@+id/text_login_pass"
        android:layout_marginTop="28dp"
        android:hint="@string/login"
        android:text="@string/login" />

</RelativeLayout>